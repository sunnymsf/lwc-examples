The `lightning/uiAppsApi` (Beta) module provides the `getNavItems` wire adapter.
`getNavItems` retrieves the items in the navigation menu.

For this module's specification and examples, see the [Lightning Web Components Developer Guide](https://developer.salesforce.com/docs/platform/lwc/guide/reference-wire-adapters-get-nav-items).
