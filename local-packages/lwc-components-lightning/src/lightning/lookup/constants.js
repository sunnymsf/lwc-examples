/**
 * Default for maximum number of lookup values
 */
export const DEFAULT_MAX_VALUES = 1;

/**
 * The desktop form factor.
 * @type {String}
 */
export const FORM_FACTOR_DESKTOP = 'Large';

/**
 * The phone form factor.
 * @type {String}
 */
export const FORM_FACTOR_PHONE = 'Small';

/**
 * The query selector used for the desktop implementation of lookups.
 * @type {String}
 */
export const LIGHTNING_LOOKUP_DESKTOP = 'lightning-lookup-desktop';

/**
 * The query selector used for the mobile implementation of lookups.
 * @type {String}
 */
export const LIGHTNING_LOOKUP_MOBILE = 'lightning-lookup-mobile';

/**
 * The error type used in LightningError AILTN log line.
 * @type {String}
 */
export const ALL_ENTITIES_SKIPPED_ERROR_TYPE =
    'LWC_LOOKUP_ALL_TARGET_ENTITIES_SKIPPED';
